{ "$schema": "https://docs.railway.app/railway.schema.json", "build": { "builder": "NIXPACKS", "buildCommand": "npm install && npm run build && cd backend && composer install --no-dev --prefer-dist && php artisan optimize", "rootDirectory": "." }, "deploy": { "startCommand": "cd backend && php artisan serve --host 0.0.0.0 --port ${PORT}", "healthcheckPath": "/", "healthcheckTimeout": 300, "restartPolicyType": "ON_FAILURE", "restartPolicyMaxRetries": 10 } }