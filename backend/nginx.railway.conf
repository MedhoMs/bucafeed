server {
    listen 8080 default_server;
    listen [::]:8080 default_server;
    
    # Root apunta a la carpeta public de Laravel dentro del contenedor
    root /var/www/public;
    index index.php index.html;

    server_name _;

    charset utf-8;

    # Logs a stdout/stderr para que Railway los capture
    access_log /dev/stdout;
    error_log /dev/stderr;

    client_max_body_size 50M;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location = /favicon.ico { access_log off; log_not_found off; }
    location = /robots.txt  { access_log off; log_not_found off; }

    # Manejo de PHP
    location ~ \.php$ {
        # En el contenedor PHP-FPM escucha por defecto en el puerto 9000 o vía socket
        # Probaremos con el puerto 9000 ya que es el estándar de la imagen php-fpm
        fastcgi_pass 127.0.0.1:9000;
        fastcgi_param SCRIPT_FILENAME $realpath_root$fastcgi_script_name;
        include fastcgi_params;
        
        # Ajustes de timeout
        fastcgi_read_timeout 300;
    }

    location ~ /\.(?!well-known).* {
        deny all;
    }
}
